// #11:Number Methods
let num1 = 23;
let num2 = 18.8;
let num3 = -6;
let output;

// Pi
output = Math.PI;

// Round
output = Math.round(num2);

// Ceil
output = Math.ceil(11.3);

// Floor
output = Math.floor(19.8);

// Sqrt
output = Math.sqrt(25);

// Abs
output = Math.abs(num3);

// Min
output = Math.min(-1, 1, 2, 3, 4, 5);

// Max
output = Math.max(-1, 2, 4, 3, 5);

console.log(output);
